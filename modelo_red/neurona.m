function z = neurona(w, x, b)

    % Calcula el polinomio de salida de una neurona
    z = (w' * x) + b;

    % Devuelve una matriz m x n donde m es el numero de muestras y n el
    % numero de neuronas
end
